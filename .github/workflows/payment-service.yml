name: CI - Payment Service

on:
  push:
    branches:
      - dev
      - 'feature/payment-service'
      - 'feature/payment*'
    paths:
      - 'Backend/Springboot/payment-service/**'
      - 'Backend/Springboot/shared-jwt/**'
  pull_request:
    branches:
      - dev
    paths:
      - 'Backend/Springboot/payment-service/**'
      - 'Backend/Springboot/shared-jwt/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven

      - name: Build Payment Service
        working-directory: Backend/Springboot/payment-service
        run: mvn clean compile -DskipTests

      - name: Test Payment Service
        working-directory: Backend/Springboot/payment-service
        run: mvn test
