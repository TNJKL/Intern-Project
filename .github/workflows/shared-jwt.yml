name: CI - Shared JWT

on:
  push:
    branches:
      - dev
      - 'feature/**'
    paths:
      - 'Backend/Springboot/shared-jwt/**'
  pull_request:
    branches:
      - dev
    paths:
      - 'Backend/Springboot/shared-jwt/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven

      - name: Build and Install Shared JWT
        working-directory: Backend/Springboot/shared-jwt
        run: mvn clean install -DskipTests
